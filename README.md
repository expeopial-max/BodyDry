# Body & Dry 🏋️‍♂️🥗

**Мобільний додаток для планування харчування та "сушки" тіла.**
Розроблено в рамках курсового проєкту для "Дніпровського Технологічного Університету «ШАГ»".

Проєкт реалізовано у форматі **Monorepo**: клієнтська частина та сервер знаходяться в одному репозиторії.

---

## 🛠 Технічний стек

### 📱 Frontend (Клієнт)
* **Framework:** React Native (Expo)
* **Language:** TypeScript
* **Styling:** StyleSheet API
* **Features:** Авторизація, Профіль, Адаптивний UI.

### 🧠 Backend (Сервер)
* **Runtime:** Node.js
* **Framework:** Express.js
* **Database:** MongoDB (Local / Atlas) + Mongoose
* **Architecture:** MVC (Model-View-Controller)
* **Security:** JWT (JSON Web Tokens)

---

### Клонування репозиторію
```bash
git clone [https://github.com/expeopial-max/BodyDry.git](https://github.com/expeopial-max/BodyDry.git)
cd BodyDry

2. Налаштування та запуск Сервера (Backend)
Сервер відповідає за базу даних та розрахунки. Його потрібно запустити першим.

Перейдіть у папку сервера:

Bash

cd server
Встановіть залежності:

Bash

npm install
Налаштування оточення (.env): Створіть файл .env у папці server і додайте туди налаштування:

Фрагмент кода

MONGO_URI=mongodb://127.0.0.1:27017/bodydry
JWT_SECRET=SuperSecretKey2026
PORT=5000
(Опціонально) Наповнення бази даних: Щоб у додатку з'явилися продукти (гречка, курка тощо), запустіть скрипт:

Bash

node utils/seed.js
Запуск сервера:

Bash

node server.js



Налаштування та запуск Додатка (Frontend)
Відкрийте новий термінал (не закриваючи сервер).

Перейдіть у папку клієнта:

Bash

cd client
Встановіть залежності:

Bash

npm install
Налаштування IP-адреси: Відкрийте файл client/app/index.tsx. Знайдіть рядок const BASE_URL і замініть IP-адресу на IPv4 вашого комп'ютера (дізнатися можна командою ipconfig у Windows або ifconfig у Mac/Linux).

TypeScript

// Приклад:
const BASE_URL = '[http://192.168.0.102:5000](http://192.168.0.102:5000)'; 
Це важливо, щоб телефон міг підключитися до вашого локального сервера.

Запуск додатка:

Bash

npx expo start